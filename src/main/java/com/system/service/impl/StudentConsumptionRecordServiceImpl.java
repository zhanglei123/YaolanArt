package com.system.service.impl;

import org.springframework.stereotype.Service;

import com.baomidou.mybatisplus.service.impl.ServiceImpl;
import com.system.mapper.StudentConsumptionRecordMapper;
import com.system.po.StudentConsumptionRecord;
import com.system.service.StudentConsumptionRecordService;

@Service
public class StudentConsumptionRecordServiceImpl extends ServiceImpl<StudentConsumptionRecordMapper, StudentConsumptionRecord> implements StudentConsumptionRecordService {

  
}
